import dpiNormalize from '../dpi-normalize'

export default {
  view: {
    padding: 10
  },

  title: {
    marginTop: 20,
    fontSize: dpiNormalize(24),
  },

  p: {
    fontSize: dpiNormalize(20),
    marginTop: 15,
    marginBottom: 15
  }
}
